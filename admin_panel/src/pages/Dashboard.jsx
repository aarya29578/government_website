import { ImagePreview } from '../components/images/ImagePreview'

export function Dashboard({ settings, firebaseConfigured, onNavigate }) {
  const configuredImages = [settings.logoUrl, settings.qrCodeUrl, settings.googlePlayUrl].filter(Boolean).length
  return <div className="dashboard-page"><div className="page-heading"><div><span className="section-kicker">OVERVIEW</span><h2>Good to see you.</h2><p>Keep your public website content current from one place.</p></div><button className="primary-button" type="button" onClick={() => onNavigate('images')}>Manage images</button></div><div className="status-grid"><article className="status-card"><span className="status-icon blue">◉</span><div><span>Website status</span><strong>Ready to publish</strong><small>Public website is connected</small></div></article><article className="status-card"><span className={`status-icon ${firebaseConfigured ? 'green' : 'orange'}`}>⌁</span><div><span>Firebase status</span><strong>{firebaseConfigured ? 'Configured' : 'Needs setup'}</strong><small>{firebaseConfigured ? 'Authentication and Firestore ready' : 'Add environment variables'}</small></div></article><article className="status-card"><span className="status-icon orange">▧</span><div><span>Image configuration</span><strong>{configuredImages}/3 configured</strong><small>Logo, QR code and app badge</small></div></article></div><section className="dashboard-section"><div className="section-title-row"><div><h3>Current website assets</h3><p>These URLs are read from siteSettings/general.</p></div><button className="text-button" type="button" onClick={() => onNavigate('settings')}>Edit settings →</button></div><div className="preview-grid"><ImagePreview label="Logo" url={settings.logoUrl} /><ImagePreview label="QR Code" url={settings.qrCodeUrl} /><ImagePreview label="Google Play" url={settings.googlePlayUrl} /></div></section></div>
}
