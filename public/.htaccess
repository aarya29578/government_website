RewriteEngine On
RewriteBase /

# Let Vite's SPA handle public routes such as /login, /signup, and /account.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
